// User inputs item and clicks submit, on submit (on Click ) get input values

// Add value to list arry, value should be formatted in a an object with
// listItem key
// order key
// complete key

//  @params
// listItem: string,
// order: num,
// complete: bool

// map through list arr and create each list item with onclick event that changes complete & incomplete value

// **** each list item should slao have an arrow that can be clicked to change the order ( click on to move up 1 )

// (list items should be displayed in order)

// append to a ul in the document


// Grab the id from the document,
// When a user clicks on a button, use the id
// document.getElementById('id').addEventListener('click', () => {
//   // Create an index variable to hold the id of clicked submit button
//   const index = parseInt(this.getElementById('id')
//   // Create a variable to hold the order number of clicked button
//   let lowerNumber = listArr[index].order;
//   // Reassign the higher item's order number by subtracting one
//   let higherNumber = lowerNumber - 1
//   // Reassign the lower item's order number by adding one
//   let lowerNumber = higherNumber + 1
//
// });
