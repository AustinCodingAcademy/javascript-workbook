'use strict';

$('[data-stack]').click(function() {
  let currentStack = $(this).children();
  let lastBlock = currentStack.last();
  let block = $(this).children().detach();

})

//get input from the '<form>' using jQuery 'submit()' method

$('form#mastermind').submit(function(data)) {
  console.log(data)
})

//set that to a variable and pass it into a function

var guess = '';
var solution = 'abed';
$('form#mastermind').submit(function(data)) {
  console.log(data)
  guess = data;
  checkForWin(guess);


//check for win

function checkForWin(guess, solution) {
  if (data.length !== solution.length) {
    return "That's not a valid guess";
  }else{
    return "You win!";
  }
}
