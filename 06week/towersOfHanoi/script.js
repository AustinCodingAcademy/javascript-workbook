'use strict';

document.addEventListener('DOMContentLoaded'), () => {
    let block = null;
    let blocksize = null;
    let lastBlock = null;
    let lastBlockSize = null;
    const totalBlocks = document.querySelector('[data-stack]').foreach(stack => stack.onclick = function(stack) {
        lastBlock = this.lastElementChild;
    })
};
