'use strict';

document.addEventListener('DOMContentLoaded', () => {
  var data = $('div[data-stack]')




});

const totalBlocks = document.querySelector('[data-stack="1"]').childElementCount;
//find number of blocks^^
//function "check win"
//manipulate the index to see if "check win" func works
//Move a block (pop/push)
//Figure out what to do when there is no block to pick up
//Rules
//Game Function
//All of these are Arrays^^



//START click functions
document.querySelectorAll('[data-stack]="1"]');

//Block

//Check win function

//Use jQuery instead, but you cannot then use "=>"
