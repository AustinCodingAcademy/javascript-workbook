function horizontalWin() {
  if (
    (board[][] === playerturn && board[][] === playerturn && board[][] === playerturn && board[][]) ||
    (board[][] === playerturn && board[][] === playerturn && board[][] === playerturn && board[][])||
    (board[][] === playerturn && board[][] === playerturn && board[][] === playerturn && board[][])
  ) {
    return true;
  } else {
    return false;
  }
}

function verticalWin() {
  if (
    (board[][] === playerturn && board[][] === playerturn && board[][] === playerturn && board[][]) ||
    (board[][] === playerturn && board[][] === playerturn && board[][] === playerturn && board[][]) ||
    (board[][] === playerturn && board[][] === playerturn && board[][] === playerturn && board[][]) ||
    ){

 }
}
