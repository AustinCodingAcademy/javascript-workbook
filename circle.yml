machine:
  node:
    version: 6.7.0
test:
  override:
    - ./node_modules/.bin/htmllint {*.html,./!(node_modules)/**/*.html}
    - ./node_modules/.bin/csslint {*.css,./**/*.css} --exclude-list=node_modules
    - ./node_modules/.bin/eslint .
