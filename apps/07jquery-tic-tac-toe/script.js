'use strict';

$(document).on('ready', function() {
  function ticTacToe(row, column) {
    board[row][column] = playerTurn(){
      turn = (turn ==='x')? 'x':'o';
    }
}
});
var turn = 'x';
playerTurn();
